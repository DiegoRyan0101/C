#include <stdio.h>
// o #include, segnifica incluindo no meu programa algo que já vem pronta, importanto uma biblioteca.
// o <stdio.h>, é uma biblioteca, ou pode ser chamada de lib de library
// std = stand, io= input/output
// <stdio.h> é uma biblioteca de entrada e saida(input e output). e captura de valores input// int é um tipo de dado. int segnifica do tipo inteiro, int = inteiro. E esse inteiro vai chamar main, função main, abre e fecha parentes segnifica executar neste momento
// main é uma função.
// printf é um metodo de saída, exibindo algo.
// minha função é do tipo main, e é do tipo int(inteiro).
// então se eu colocar do tipo int(inteiro) eu tenho que retornar um número inteiro, que é o, return 0;
// o, return 0; o 0 vai falar para o software que deu tudo certo na minha execução.
// abertura  e fechamento de chaves {} , oque está dentro da abertura e fechamento de chaves, chamamos de escopo, qual é o escopo da minha função main, é tudo que estiver dentro da abertura e fechamento das chaves.
// o escopo é o corpo da função é oque ela vai fazer.
// printf, é imprimir algo, mostrar algo na tela, exibir. o f é de formatado. um texto formatado.
// abertura e fechamento de parenteses junto com o printf, e dentro dele entre aspas duplas, será exibido uma string.
// quando escrevemos um conteúdo entre aspas, é chamado de string. string = um conjunto de caracteres.
// o ponto e vírgula no fim de um código, segnifica o fim de uma instrução
// o return 0; é sempre que declaramos uma função do tipo um inteiro, esse return está entregando para a função um valor. então ele não precisa retornar nada alem de zero. o zero segnifica que deu tudo certo o código.
int main()
{
    printf("Hello World!");
    return 0;
}
